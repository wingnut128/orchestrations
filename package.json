{
  "name": "orchestrations",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "temporal:dev": "temporal server start-dev",
    "worker:greeter": "bun run src/workers/greeter.ts",
    "client:greeter": "bun run src/clients/run-greeter.ts",
    "worker:agent-task": "bun run src/workers/agent-task.ts",
    "client:agent-task": "bun run src/clients/run-agent-task.ts",
    "worker:ci-pipeline": "bun run src/workers/ci-pipeline.ts",
    "client:ci-pipeline": "bun run src/clients/run-ci-pipeline.ts",
    "check": "biome check",
    "check:fix": "biome check --write",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@temporalio/activity": "^1.15.0",
    "@temporalio/client": "^1.15.0",
    "@temporalio/worker": "^1.15.0",
    "@temporalio/workflow": "^1.15.0"
  },
  "trustedDependencies": [
    "@swc/core",
    "protobufjs"
  ]
}
